// style imports
import "./style.scss";

const ChatInput = () => {
  return (
    <div className="chat-input">
      <input type="text" placeholder="Type a message..." />
    </div>
  );
};

export default ChatInput;
